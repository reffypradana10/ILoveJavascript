// forEach is a Method used to iterate to iterate over the elements of an array an d apply a specified function (callback) to each element

// forEach adalah sebuah metode pada array di JavaScript yang digunakan untuk
// mengiterasi setiap elemen dalam array tersebut dan menerapkan fungsi (callback) yang telah ditentukan ke setiap elemen.

// array.forEach(callback)
// element, index, array are provided

// let number = [1, 2, 3, 4];

// number.forEach(double);
// number.forEach(display);

// function double(e, i, arr) {
//   arr[i] = e * 2;
// }

// function triple(e, i, arr) {
//   arr[i] = e * 3;
// }

// function square(e, i, arr) {
//   arr[i] = Math.pow(e, 2);
// }

// function display(e) {
//   console.log(e);
// }
